import React from 'react';
import PropTypes from 'prop-types';
import MenuLeft from './left/left';

Menu.propTypes = {
    
};

function Menu(props) {
    return (
        <nav className="Header__MainNav hidden-pocket hidden-lap" aria-label="Main navigation">
            
        </nav>
    );
}

export default Menu;