import React from 'react';
import Main from './main/main';
import './header.css'

Header.propTypes = {
};

function Header(props) {
    return (
        <Main />
    );
}

export default Header;